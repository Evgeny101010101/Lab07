#include <string>
#include <fstream>
#include <iostream>

void print(const std::string& text, std::ostream& out = std::cout);  	# Print1
void print(const std::string& text, std::ofstream& out);		# Print2	
